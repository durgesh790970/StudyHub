run = "cd backend && python manage.py runserver 0.0.0.0:5000"

[[ports]]
localPort = 5000
externalPort = 80

[deployment]
deploymentTarget = "autoscale"
run = ["sh", "-c", "cd backend && python -m gunicorn djproject.wsgi:application --bind 0.0.0.0:5000"]
